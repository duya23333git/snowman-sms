server:
  port: 6868

eureka:
  client:
    # 是否将自己注册到Eureka中
    register-with-eureka: false
    # 是否从eureka中获取信息
    fetch-registry: false
    service-url:
      defaultZone: http://192.168.3.48:${server.port}/eureka/